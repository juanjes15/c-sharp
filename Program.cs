float n1, n2, n3, n4, n5, res;

for (int i = 0; i < 5; i++)
{
    Console.WriteLine("Ingrese una nota");
    n(i) = float.Parse(Console.ReadLine());
}

for (int i = 1; i < 6; i++)
{
    if (i < 3)
    {
        res = res + (ni * 0, 125);
    }
    else
    {
        res = res + (ni * 0, 25);
    }
}

Console.WrileLine("La nota final es: " + res);